{
  "name": "elif-oficina",
  "version": "1.0.0",
  "private": true,
  "description": "Site estático da Oficina ELIF",
  "scripts": {
    "start": "npx --yes serve . -l 3000",
    "build": "node scripts/build-dist.js",
    "preview": "npm run build && wrangler dev --config wrangler.toml",
    "deploy": "npm run build && wrangler deploy --config wrangler.toml"
  },
  "dependencies": {
    "wrangler": "^4.127.0"
  }
}
